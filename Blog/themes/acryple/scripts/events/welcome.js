hexo.on('ready', () => {
  const { version } = require('../../package.json')
  hexo.log.info(`Hexo Acryple theme based Butterfly ${version}
============================================================`)
})